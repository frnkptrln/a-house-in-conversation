# Recovery review — 8 September 2026

Reviewed baseline: `c6164e0` on main. The recovery in #17 is already merged, as
are #18's mobile-caption correction and #19's touch/layout correction. The V2
replacement in #16 remains withdrawn. Diffing against pre-V2 `74737e9` confirms
that the original room implementations are present; the Afterimage narration
removal and later small recovery fixes are retained.

## Observed in the existing public house

URL: https://frnkptrln.github.io/a-house-in-conversation/

Desktop Chromium viewport: 1363 × 936. Loaded threshold scripts were
`house-age.js?v=1.4.0`, `threshold-rooms.js?v=1.4.0`, `script.js?v=1.4.3`, matching
the recovery baseline. This was a read-only check of the already published work.

| Check | Observed result |
| --- | --- |
| Cross threshold | Original open-rooms entrance and all eight room links appear. |
| Sound toggle | `silence`/`listen` changes state; canvas remains at x=0, y=0, width=1363, height=936, with zero page scroll. |
| Listening entrance | Original text and `take a place` appear; activation reaches Spatial listening performance and “Distant voices are present.” |
| Return from Listening | `#rooms` restores the open-rooms view; the canvas retains its original bounds. |
| Colour entrance and start | Original “bring two colours together” entrance; `enter` reaches Interactive colour performance. |
| Colour completion | The existing score reaches “neither contained this”, with `again` and the return link. |
| Afterimage | Original 92-second entrance; activation reaches the sound-work region and “The room is sounding.” The source remains free of speech synthesis; the score reaches the original ending with `again` and the return link. |

These observations establish navigation and visible playback state; they do not
certify acoustic quality. No full Listening score or mobile Safari listening
review was performed. Browser extension metadata errors were observed; no house
application console error was identified during the checked interactions.

## Local verification and boundary

The repository's JavaScript syntax and local HTML/CSS asset references were
checked. This review changes no existing production HTML, JavaScript, CSS, audio,
room compositions, ageing or stored data. It does not reset the house or publish
a new version. Main's restoration is the reference for further work.

The available browser blocks the local preview server. The separate
[threshold residue study](../studies/threshold-residue/README.md) is therefore
delivered for viewing, with static/syntax validation only. A real mobile Safari
check of tap, sound toggle, caption separation and return navigation remains the
specific missing device step; the already merged fixes are not reclassified as
unimplemented.
